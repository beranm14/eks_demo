apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sayhi
  namespace: app
spec:
  rules:
    - host: sayhi.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sayhi
                port:
                  number: 3000

